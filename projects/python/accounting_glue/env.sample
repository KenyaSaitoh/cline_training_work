# ETL環境設定ファイル（例）
# このファイルをコピーして、実際の環境変数として設定してください
# Windows: set 変数名=値 または PowerShell: $env:変数名="値"
# Linux/Mac: export 変数名=値

# ==================================================
# 実行環境設定
# ==================================================
# 'local': ローカル開発環境（HsqlDB/PostgreSQL）
# 'aws': AWS本番環境（S3 + Glue）
ETL_ENVIRONMENT=local

# ==================================================
# ソースデータベース接続設定（ローカル環境用）
# ==================================================

# Hsqldb用設定（デフォルト）
SOURCE_DB_DRIVER=org.hsqldb.jdbc.JDBCDriver
SOURCE_DB_URL=jdbc:hsqldb:hsql://localhost:9001/testdb
SOURCE_DB_HOST=localhost
SOURCE_DB_PORT=9001
SOURCE_DB_NAME=testdb
SOURCE_DB_USER=SA
SOURCE_DB_PASSWORD=
HSQLDB_JAR_PATH=../../hsqldb/lib/hsqldb.jar

# PostgreSQL用設定（本番環境例）
# SOURCE_DB_DRIVER=org.postgresql.Driver
# SOURCE_DB_URL=jdbc:postgresql://prod-db.example.com:5432/production
# SOURCE_DB_HOST=prod-db.example.com
# SOURCE_DB_PORT=5432
# SOURCE_DB_NAME=production
# SOURCE_DB_USER=etl_user
# SOURCE_DB_PASSWORD=your_secure_password

# AWS RDS用設定（本番環境例）
# SOURCE_DB_DRIVER=org.postgresql.Driver
# SOURCE_DB_URL=jdbc:postgresql://prod-rds.xxxxx.us-east-1.rds.amazonaws.com:5432/prod_db
# SOURCE_DB_HOST=prod-rds.xxxxx.us-east-1.rds.amazonaws.com
# SOURCE_DB_PORT=5432
# SOURCE_DB_NAME=prod_db
# SOURCE_DB_USER=prod_etl_user
# SOURCE_DB_PASSWORD=prod_password

# ==================================================
# ターゲットデータベース接続設定（ローカル環境用）
# ==================================================

# Hsqldb用設定（デフォルト - ソースと同じ）
TARGET_DB_DRIVER=org.hsqldb.jdbc.JDBCDriver
TARGET_DB_URL=jdbc:hsqldb:hsql://localhost:9001/testdb
TARGET_DB_HOST=localhost
TARGET_DB_PORT=9001
TARGET_DB_NAME=testdb
TARGET_DB_USER=SA
TARGET_DB_PASSWORD=

# 別のデータベースをターゲットにする場合は、上記の設定を変更してください

# ==================================================
# AWS設定（本番環境用）
# ==================================================

# S3バケット名
# S3_BUCKET=your-etl-bucket-name

# AWS リージョン
# AWS_REGION=us-east-1

# Glueジョブ設定
# GLUE_JOB_NAME=accounting-etl-job
# GLUE_DATABASE=accounting_catalog

# ==================================================
# ETLジョブパラメータ
# ==================================================

# バッチサイズ
# BATCH_SIZE=10000

# エラー閾値
# ERROR_THRESHOLD=100

# ログレベル (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

